{% extends 'base.html.twig' %}

{% block title %}ClassNames index{% endblock %}

{% block body %}
    <h1>ClassNames index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for class_name in class_names %}
            <tr>
                <td>{{ class_name.id }}</td>
                <td>{{ class_name.name }}</td>
                <td>
                    <a href="{{ path('class_names_show', {'id': class_name.id}) }}">show</a>
                    <a href="{{ path('class_names_edit', {'id': class_name.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('class_names_new') }}">Create new</a>
{% endblock %}
