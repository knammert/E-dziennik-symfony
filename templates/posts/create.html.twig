@extends('layouts.master')

@section('contentPage')
<div class="row">

    <div class="col-md-8 col-md-offset-2 shadow-lg p-3 mb-5 bg-white rounded">

        <h1>Nowy post</h1>

        <form action="{{ route('dashboard.store') }}" method="POST" enctype="multipart/form-data">
            @csrf
            <div class="form-group">
                <label for="title">Tytuł</label>
                <input type="text" class="form-control" name="title" />
                @error('title')
                    <div class="invalid-feedback d-block">{{ $message }}</div>
                @enderror
            </div>

            <div class="form-group">
                <label for="description">Treść</label>
                <textarea rows="5" class="form-control" name="description" ></textarea>
                @error('description')
                    <div class="invalid-feedback d-block">{{ $message }}</div>
                @enderror
            </div>


            <div class="form-group">
                <div class="custom-file">
                    <input type="file" class="custom-file-input file" name='file' id="file" required>
                    <label class="custom-file-label" for="file">Wybierz plik</label>
                    <div class="invalid-feedback">Wybrano zły plik</div>
                  </div>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">
                    Stwórz nowy post
                </button>
                <button class="btn btn-default">
                    Anuluj
                </button>
            </div>
        </form>
    </div>

</div>


<script>
    $('#file').on('change',function(){
        //get the file name
        var fileName = $(this).val();
        //replace the "Choose a file" label
        $(this).next('.custom-file-label').html(fileName);
    })
</script>
@endsection
